{
  "Jwt": {
    "//Key": [
      "Development-only signing key, committed so the project runs immediately",
      "after checkout. It signs nothing of value: tokens issued with it are only",
      "accepted by a Development-environment API running on this same key.",
      "Startup FAILS outside Development unless Jwt__Key is set in the",
      "environment — see backend/.env.example."
    ],
    "Key": "development-only-signing-key-do-not-use-in-production-0123456789"
  },

  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.AspNetCore": "Information",
        "Microsoft.EntityFrameworkCore.Database.Command": "Information"
      }
    }
  }
}
